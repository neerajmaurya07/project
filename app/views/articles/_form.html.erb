<%= form_for (@article) do |f| %>
	<p><%= f.label :title %>
	<%= f.text_field :title %></p><br>
	<p>
	<%= f.label :body %>
	<%= f.text_area :body %></p><br>
	<p><%= f.submit "Submit"%></p>

<% end -%>